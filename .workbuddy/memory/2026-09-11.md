# 2026-09-11

- 运行 interview 项目（Vite + Vue3 + Element Plus）本地预览：`npm run dev -- --host 127.0.0.1 --port 5173`，Vite 5.4.21 启动正常，首页与全部 src 模块请求均 200，无编译错误。
- 修复题目代码块渲染（核心 bug）：题库 TS 模板字符串里反引号被写成 `\\\`` 三重转义，运行时输出「\ + 反引号」，Markdown 围栏识别失败，整段代码平铺成正文。
  - 数据侧：`scripts/fix-escaped-backticks.cjs` 按「连续反斜杠运行长度」分类修复，14 个文件、420 处围栏 + 168 处行内 + 112 处 `\${`；刻意保留 3 处「Windows 用 `\`」的真实反斜杠。改前已备份到 `.workbuddy/backup/data-20260911/`。
  - 渲染侧：新建 `src/composables/markdownRenderer.ts` 作为唯一渲染器。把惰性正则 ```` /```([\s\S]*?)```/ ```` 换成按行的 CommonMark 围栏解析（修复「代码示例内部讨论 ``` 时代码被提前截断」），加未闭合围栏兜底、`\`\`\`` 归一化、代码块头部语言标签 + 复制按钮。
  - QuestionPanel.vue 与 QuestionManager.vue 此前各有一份 simpleMarkdown 副本，已统一改为引用该 composable。
  - `DATA_VERSION` 升到 v1.0.3 以失效 localStorage 旧缓存。
  - 校验：`scripts/test-renderer.cjs` 10 项用例全通过，599 题渲染出 1075 个 `<pre>` 代码块，无占位符残留；vue-tsc + vite build 通过。
  - 新增 npm scripts：`fix:escapes` / `check:fences` / `test:render`。
- 项目约定：题库内容写在 TS 模板字符串里，反引号只能写 `\``（单反斜杠），写成 `\\\`` 就会渲染出可见的反斜杠。
- 移除「?version=full 才展示完整题目」的分版逻辑（App.vue）：删除 `isFullVersion` / `LITE_MAX_ID=720` 过滤、heroStats 改为按实际数据动态计算、localStorage key 去掉 `_full`/`_lite` 后缀并加 `readWithLegacy()` 兼容迁移旧进度。vue-tsc --noEmit 通过。
- 环境注意：本机 Bash 工具默认 PATH 缺失，`ls`/`dirname` 等命令不可用，需先 `export PATH="/c/Program Files/nodejs:/usr/bin:/bin:$PATH"` 再执行命令；PowerShell 工具当前无回显输出，建议优先用 Bash。
